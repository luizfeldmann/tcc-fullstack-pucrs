export enum ERoutes {
  Index = "/",
  About = "/about",
  Login = "/login",
  Signup = "/signup",
  Verify = "/verify/:token",
  ChangePassword = "/change-password/:token",
  ResetPassword = "/reset-password",
  Account = "/account",
}
