export enum ERoutes {
  Index = "/",
  About = "/about",
  Login = "/login",
  Logout = "/logout",
  Signup = "/signup",
  Verify = "/verify/:token",
  ChangePassword = "/change-password/:token",
  ResetPassword = "/reset-password",
  Account = "/account",
  Transactions = "/transactions",
  Deposit = "deposit",
}
